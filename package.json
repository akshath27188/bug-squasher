{
  "//": "This file configures the Node.js server that acts as the backend for the browser extension.",
  "name": "bug-squasher-ai-server",
  "version": "1.0.0",
  "description": "Server for Bug Squasher AI",
  "main": "server.js",
  "scripts": {
    "//": "Use 'npm start' to run the backend server locally for testing.",
    "start": "node server.js",
    "//": "IMPORTANT: Run 'npm run build' after ANY change to .ts/.tsx files to compile them into JavaScript for the extension.",
    "build": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@google/genai": "^1.20.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@types/node": "^20.0.0"
  }
}
